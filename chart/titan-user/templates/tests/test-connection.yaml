apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "titan-user.fullname" . }}-test-connection"
  labels:
    {{- include "titan-user.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "titan-user.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
