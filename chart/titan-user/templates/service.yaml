apiVersion: v1
kind: Service
metadata:
  name: {{ include "titan-user.fullname" . }}
  labels:
    {{- include "titan-user.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "titan-user.selectorLabels" . | nindent 4 }}
